/**
 * Obras sociales y prepagas con las que se trabaja.
 *
 * PENDIENTE: completar con la lista real confirmada por la Dra. Mientras el
 * array esté vacío, la sección muestra un mensaje genérico invitando a
 * consultar la cobertura, en lugar de afirmar algo que no se verificó.
 *
 * Ejemplo:
 *   export const COBERTURAS = ["OSDE", "Swiss Medical", "Galeno", "Medifé"];
 */
export const COBERTURAS: string[] = [];
