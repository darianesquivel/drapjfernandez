---
import Header from "../components/header/Header.astro";
import Footer from "../components/footer/Footer.astro";
import "../styles/global.css";
import WhatsappBtn from "../components/WhatsappBtn.astro";

const {
  title = "Dra. Pamela J. Fernandez - Cirujana Gral. Bariátrica y Percutánea",
  description = "Especialista en cirugía bariátrica, general y percutánea.",
} = Astro.props;
---

<!doctype html>
<html lang="es" class="scroll-smooth">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="index, follow" />
    <link rel="canonical" href="https://www.drapjfernandez.com.ar" />

    <title>{title}</title>
    <meta name="description" content={description} />

    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://www.drapjfernandez.com.ar" />
    <meta
      property="og:image"
      content="https://www.drapjfernandez.com.ar/og-image.png"
    />
    <meta name="twitter:card" content="summary_large_image" />

    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <link
      rel="preload"
      as="image"
      href="/hero-image.webp"
      fetchpriority="high"
    />

    <link
      rel="stylesheet"
      href="https://unpkg.com/aos@next/dist/aos.css"
      media="print"
      onload="this.media='all'"
    />
  </head>

  <body class="bg-brand antialiased text-brand-5">
    <Header />
    <main class="flex flex-col min-h-screen">
      <slot />
    </main>
    <Footer />

    <WhatsappBtn />

    <script>
      const initAOS = async () => {
        const { default: AOS } = await import("aos");
        AOS.init({
          duration: 600,
          offset: 100,
          once: true,
        });
      };

      const idleCallback =
        window.requestIdleCallback || ((cb) => setTimeout(cb, 2000));

      if (document.readyState === "complete") {
        idleCallback(initAOS);
      } else {
        window.addEventListener("load", () => idleCallback(initAOS));
      }

      document.addEventListener("astro:after-swap", initAOS);
    </script>
  </body>
</html>
